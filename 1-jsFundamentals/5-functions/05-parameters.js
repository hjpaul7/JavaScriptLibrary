//a way to have a function to take in information

//      keyword     name        parameters
        function    hi          (name) {
            console.log(`hi, ${name}`);
        }
        hi('Hustin');
        hi(800); //numerics work too

//challenge

function name (first, last) {
    console.log(`Hello, my name is ${first} ${last}`);
}
name('Hustin', 'Jeffers');